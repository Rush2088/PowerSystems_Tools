# Power System Tools

Combined React app containing:

- Fault Level Calculator
- Grid Source Impedance Calculator

## Run locally

```bash
npm install
npm run dev
```

## GitHub Pages

This project uses `HashRouter`, so it works well on GitHub Pages project sites.

Update `vite.config.js` `base` if you prefer a fixed repo-name base, or keep `./` for a portable build.
